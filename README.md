ansible-playbook-ppa-dep
====================

A very tiny ansible playbook that can be included as a submodule role for installing ppa keys with apt. 
All it does is install the python-software-properties package.
